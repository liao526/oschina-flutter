import 'package:flutter/material.dart';

class NewsMainWidget extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Center(
          child: Text("综合"),
        );
    }
}
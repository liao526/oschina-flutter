import 'package:flutter/material.dart';

class TweetMainWidget extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Center(
          child: Text("动弹"),
        );
    }
}
